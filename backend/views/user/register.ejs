<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register User</title>
    <link rel="stylesheet" href="<%= url %>/css/output.css" />
  </head>
  <body>
    <div class="flex flex-col justify-center items-center h-screen">
      <h1 class="text-4xl pb-10 font-bold">Admin Panel</h1>
      <div
        class="h-[460px] w-[400px] bg-gray-500 flex flex-col items-center rounded-md p-3"
      >
        <h1 class="text-3xl py-7 font-bold text-white">Register User</h1>
        <form action="/user/register" method="POST">
          <label for="Name" class="text-sm font-medium text-white">Name</label>
          <input
            type="text"
            name="name"
            placeholder="Name"
            class="w-full rounded-md p-2 my-2 bg-gray-50 border border-gray-300 text-gray-900"
            required
          />
          <label for="Name" class="text-sm font-medium text-white">Email</label>
          <input
            type="email"
            name="email"
            placeholder="Email"
            class="w-full rounded-md p-2 my-2 bg-gray-50 border border-gray-300 text-gray-900"
            required
          />
          <label for="Name" class="text-sm font-medium text-white"
            >Password</label
          >
          <input
            type="password"
            name="password"
            placeholder="Password"
            class="w-full rounded-md p-2 my-2 bg-gray-50 border border-gray-300 text-gray-900"
            required
          />
          <button
            type="submit"
            class="w-full bg-slate-600 py-3 mt-6 rounded-md text-white hover:bg-slate-400"
          >
            Register
          </button>
          <p class="mt-3">
            Already have an account ?
            <a
              href="/api/login"
              class="py-3 text-blue-700 font-medium hover:text-blue-400 cursor-pointer shadow-sm"
              >Login</a
            >
          </p>
        </form>
      </div>
    </div>
  </body>
</html>
